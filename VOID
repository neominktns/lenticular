// LEFT EYE - increase from 50 to 80
if (leftEyeDist < 80) {  // ← BIGGER radius
    const eyeInfluence = Math.max(0, 1 - leftEyeDist / 80);  // ← Match it
    const smoothInfluence = eyeInfluence * eyeInfluence * eyeInfluence;  // ← CUBE
    // rest of code...
} 



const smoothInfluence = Math.exp(-(eyeDist * eyeDist) / (2 * 25 * 25));  // Gaussian blur curve
